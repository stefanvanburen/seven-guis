image: archlinux
packages:
  - nodejs
  - npm
  - jre-openjdk-headless
  - hut
oauth: pages.sr.ht/PAGES:RW
environment:
  site: seven-guis
tasks:
  - build: |
      cd $site
      npm install
      npm run build
